# Core pipeline (Apple Silicon required — parakeet-mlx / mlx-whisper are MLX-only)
parakeet-mlx>=0.5.2
mlx-whisper>=0.4.3
pyannote.audio>=4.0.7
torch>=2.12
torchaudio>=2.11
soundfile>=0.14
numpy>=2.2,<2.3
numba>=0.60
punctuators>=0.0.7

# Export + menu-bar app
python-docx>=1.2
rumps>=0.4

# Tests
pytest>=9

# Optional: accuracy evaluation against reference transcripts (tuning/)
# jiwer>=4.0
# meeteval>=0.4
